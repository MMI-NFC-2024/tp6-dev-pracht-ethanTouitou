---
import "../styles/global.css";

const {
  title = "Projet Astro",
  description = "TP rendu dynamique et statique avec Tailwind",
  children,
} = Astro.props;
---

<html lang="fr" class="scroll-smooth bg-gray-50 text-gray-900">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width" />
    <title>{title}</title>
    <meta name="description" content={description} />
  </head>

  <body class="flex flex-col min-h-screen font-sans">
    <!-- Navbar -->
    <header class="bg-white shadow-sm sticky top-0 z-50">
      <nav
        class="max-w-6xl mx-auto flex justify-between items-center px-6 py-4"
      >
        <a
          href="/"
          class="text-2xl font-semibold text-blue-600 hover:text-blue-700 transition-colors"
        >
          🐧 Astro TP
        </a>

        <ul class="flex gap-6 text-gray-700 font-medium">
          <li>
            <a
              href="/rendu-dynamique"
              class="hover:text-blue-600 transition-colors">Rendu dynamique</a
            >
          </li>
          <li>
            <a
              href="/rendu-statique"
              class="hover:text-blue-600 transition-colors">Rendu statique</a
            >
          </li>
        </ul>
      </nav>
    </header>

    <!-- Contenu principal -->
    <main class="flex-grow max-w-6xl mx-auto w-full px-6 py-10">
      <slot />
    </main>

    <!-- Footer -->
    <footer
      class="bg-gray-100 text-center py-6 text-sm text-gray-600 border-t border-gray-200"
    >
      Réalisé avec ❤️ par Noah — BUT MMI2
    </footer>
  </body>
</html>
