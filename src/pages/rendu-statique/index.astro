---
import penguins from "../../assets/penguins.json";

const speciesList = [...new Set(penguins.map((p) => p.species))].sort();
---

<h1>Liste des espèces (rendu statique)</h1>

<ul>
    {
        speciesList.map((s) => (
            <li>
                <a href={`/rendu-statique/${s}`}>{s}</a>
            </li>
        ))
    }
</ul>
